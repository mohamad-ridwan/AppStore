import { memo } from "react";

type Props = {
    images?: string[]
}

const ImgProductSwiper = memo(({
    images = []
}: Props)=>{
    return (
        <></>
    )
})

export default ImgProductSwiper